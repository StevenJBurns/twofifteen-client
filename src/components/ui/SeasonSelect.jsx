import React from 'react';
// import { PropTypes } from 'prop-types';
import './SeasonSelect.scss';

export const SeasonSelect = props => {
  return (
    <ul className="season-select"></ul>
  );
};

export default SeasonSelect;